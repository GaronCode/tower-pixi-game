export default class BaseBullet {
    constructor({app, speed, damage, size, textureName}) {
        this.speed = speed;
        this.damage = damage;
        this.textureName = textureName;
        this.size - size;
    }
    
}